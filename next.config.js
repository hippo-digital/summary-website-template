/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    trailingSlash: false,
    compiler: {
        styledComponents: true,
    },
};

module.exports = nextConfig;
